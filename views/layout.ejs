<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Emlak Uzmanınız</title>
    <link rel="preconnect" href="https://drive.google.com" crossorigin>
    <link rel="preconnect" href="https://lh3.googleusercontent.com" crossorigin>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/assets/site.css" />
    <link rel="stylesheet" href="/assets/theme.css" />
    <style>
        :root {
            --primary-color: #2E86AB; /* Ana Renk */
            --secondary-color: #1ABC9C; /* İkincil Renk */
            --light-gray: #F5F7FA; /* Arka plan */
            --text-dark: #2C3E50; /* Metin */
            --nav-link: #2C3E50;
            --nav-link-hover: #1A5276;
            --nav-brand: #2C3E50; /* Başlık / Vurgu */
            --border-color: #E0E0E0;
        }
        
        html, body { height: 100%; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: var(--text-dark);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        .navbar {
            background: var(--light-gray);
            border-bottom: 1px solid var(--border-color);
            box-shadow: none;
        }
        .navbar .navbar-brand {
            color: var(--nav-brand);
        }
        .navbar .nav-link {
            color: var(--nav-link);
        }
        .navbar .nav-link:hover,
        .navbar .nav-link:focus,
        .navbar .nav-link.active {
            color: var(--nav-link-hover);
        }
        
        .hero-section {
            background: linear-gradient(135deg, var(--secondary-color), #ffffff);
            padding: 80px 0;
        }
        
        .property-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
            overflow: hidden;
        }
        
        .property-card:hover {
            transform: translateY(-5px);
        }
        
        .btn-primary {
            background: var(--primary-color);
            border: none;
            border-radius: 25px;
            padding: 10px 30px;
        }
        
        .btn-primary:hover {
            background: #64B5F6;
        }
        
        .footer {
            background: var(--text-dark);
            color: white;
            padding: 40px 0;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
                <i class="fas fa-home me-2"></i>Hacı Polat
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Ana Sayfa</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/properties">İlanlar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">İletişim</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/login">Giriş</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main style="flex: 1 0 auto;">
        <%- body %>
    </main>

    <!-- Footer intentionally omitted in layout -->

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= process.env.GOOGLE_API_KEY %>&libraries=places"></script>
</body>
</html>
